Dialogs:
  com:
    axonivy:
      connector:
        deepl:
          demo:
            translateTextAdvanced:
              BySettingToHtml: By setting tag_handling to html, the API will process HTML input by extracting the text, splitting it into individual sentences, translating them, and placing them back into the HTML structure
              LeavingTheField: Leaving the field empty will automatically detect the source language.
              SelectSource: Select source language
              SelectTarget: Select target language
              TagHandling: Tag Handling
